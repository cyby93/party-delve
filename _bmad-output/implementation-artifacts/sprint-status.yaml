# generated: 2026-06-22
# last_updated: 2026-07-26 (7-7a-boss-charged-delta-contract-and-broadcast → done)
# project: party-delve
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
#
# EPIC vs PHASE NUMBERING:
# ========================
# GDD uses Epic numbering (E1–E10) for feature content milestones.
# CLAUDE.md uses Phase numbering (Phase 1–5) for technical milestones.
# Phase 1 (Foundation Platform) ≈ E1; Phase 2 (Local Party MVP) spans E1→E2.
# Phase2 legacy stories (old Phase-based keys) are tracked below under PHASE 2 STORIES.

generated: 2026-06-22
last_updated: 2026-07-26 (7-7b-grassland-boss-attack-vfx-and-charge-visual → done)
# 2026-07-26: 7-7b-grassland-boss-attack-vfx-and-charge-visual → done. User (Cyby)
# confirmed Task 8.4's manual Client-UX pass live and gave explicit sign-off to close.
# 2026-07-26: code review of 7-7b-grassland-boss-attack-vfx-and-charge-visual (was left at
# review, not done, pending the above) — self-run Blind Hunter + Edge Case Hunter + Acceptance Auditor,
# review_mode: full. 10 raw findings → 4 patch (all applied: Number.isFinite guards on
# planBossCharged/planBossStomped, File List fix, NaN-sweep test widened to array colors,
# bossDamageVfxAtRef reset added to unmount cleanup), 2 defer (D-7.7b-A/B in
# deferred-work.md), 4 dismissed as noise after verification. Typecheck + full suite
# re-confirmed clean after patching. Status intentionally stays review: Task 8.4's manual
# Client-UX pass is still outstanding (no display in this sandbox), matching this
# project's established precedent (7.5, 7.6, 3.23, dev-3).
# 2026-07-26: implemented 7-7b-grassland-boss-attack-vfx-and-charge-visual (review) —
# 7.7a had shipped at this baseline (BossChargedDelta exists, GameRoom.ts broadcasts it),
# so all 8 tasks done in full, not the degraded path. New boss-vfx.ts pure planner (5
# tests) drives all four boss reactions (charged/stomped/phaseChanged/damaged) from the
# 7.1 primitive library via a new applyBossVfxPlan bridge in DungeonScreen.tsx: replaced
# the raw 66ms boss:stomped Graphics ring, gave boss:phaseChanged its first-ever on-screen
# effect, added a throttled boss:damaged impact burst, wired the new boss:charged post-hoc
# dash visual end-to-end (planner + host-session.ts whitelist + DungeonScreen branch).
# One deliberate deviation from the story's literal Task 7.1 code sample: applyBossVfxPlan
# threads Date.now() as startedAt into every primitive (BACKGROUNDED-TICKER rule, matching
# spawnSouldrinkerVfx/triggerSpiritcallerCast) since these branches trigger from a
# non-RAF-gated useEffect — the story text didn't call this out but established codebase
# convention did. Typecheck clean (10/10 tsconfigs), full suite 83 passed / 1 pre-existing
# failure (ability-vfx.test.ts Stone Wall centering, confirmed via git stash against
# baseline, unrelated). Status set to review, not done: Task 8.4's Client-UX manual pass
# was not performed — no display in this sandbox, matching established precedent (7.5,
# 7.6, 3.23, dev-3) — needs a human pass before the story can be closed.
# 2026-07-26: 7-6-status-effect-vfx → done. User ran the live Client-UX pass (Task
# 7.1/7.2) themselves: all four effect shapes/colors confirmed correct. One real
# finding — Warding Cry's shield aura was visible but not visibly pulsing — root-
# caused to SHIELD_ALPHA_BASE (0.22) being too low-contrast for a filled/unstroked
# shape versus the other three effects' 0.55-0.95 peak alpha; fixed by bumping to
# 0.45 in status-aura.ts, user re-confirmed working live. Separately clarified (not
# a VFX bug, not changed): the caster never sees the shield on themselves when
# casting Warding Cry because GameRoom.ts's gatherPlayersInHitZone excludes the
# caster from allies-in-zone targeting (pre-existing Story 3.17 simulation
# behavior, Simulation-Engineer-owned, out of this story's scope). Task 7.3-7.6
# (stacking/reconnect/down-spirit/kill) were not individually walked in the
# reported session; user gave explicit sign-off to close the story regardless.
# 2026-07-25: code review of 7-6-status-effect-vfx (stays review, not done) — Blind Hunter
# subagent ran clean; the Edge Case Hunter and Acceptance Auditor subagents both hit a
# session usage-limit failure mid-run and could not be retried, so that analysis was
# performed directly instead (disclosed in the story's Review Findings section, not
# claimed as independent-subagent output). 2 patches applied: a MAX_STATUS_AURAS
# ceiling-denial edge case that could permanently strand a stale aura handle (self-
# reinforcing ratchet under sustained >64-concurrent-aura load, well above the ~44
# documented worst case but a real correctness gap), and 2 dead imports. 11 other Blind
# Hunter findings dismissed as noise after verification (down/spirit "missing filter" and
# "dropped isFrozen dim" both matched pre-existing/spec-mandated behavior; setChildIndex
# usage matched the established tethers/zones/bodies idiom, not the different Iron-Skin
# pattern a past review actually rejected; several claims were disproven by reading
# engine.ts/primitives.ts, which the blind layer had no access to). All 6 ACs individually
# re-audited: PASS. Status intentionally left at review, not done — code review is clean
# but Task 7's human Client-UX pass (CLAUDE.md Merge Gate) is still outstanding.
# 2026-07-25: implemented 7-6-status-effect-vfx (review) — new status-aura.ts (pure
# spec/factory, 13 tests) drives all 4 status-effect types (damageReduction ring,
# shield dome, damageBuff burst, slow orbiting trail) from the 7.1 primitive library,
# replacing DungeonScreen's flat-colour badge. Composition-rule call: removed 7.2's
# Iron Skin persistent shell and 7.3's Warding Cry persistent shield pulse outright
# (both were the same "persist-for-duration" visual this story generalizes, per the
# story's own Dev Notes; neither had a separate cast-instant worth keeping) — 7.4's
# Dark Pact onset was left alone since it was already only a one-shot cue. Typecheck
# clean (10/10 tsconfigs), full suite 555 passed / 2 pre-existing failures (confirmed
# via git stash against the pre-story baseline) / 3 skipped. deferred-work.md's D-7.1-D
# updated with this story's concrete concurrent-handle (~44 worst case) and allocation-
# rate (~34/s) verdict — pooling stays deferred. Status set to review, not done: Task 7's
# Client-UX manual pass (live host + phones, couch-distance legibility) was not performed
# — no display in this sandbox, matching this project's established precedent (7.5, 3.23,
# dev-3) — needs a human pass before the story can be closed.
# 2026-07-22: created the remaining Epic 7 stories in one batch (7.2-7.6, 7.7a, 7.7b, 7.8).
# Story 7.7 was split into 7.7a (protocol+sim) and 7.7b (host) — see the E7 block below.
# Three epic-text claims were corrected against the shipped code during creation:
# (1) boss:charged is not a whitelist fix — the delta type does not exist (7.7a);
# (2) ZoneState has no class/abilityIndex fields, only ProjectileState does, so 7.8 derives
#     zone identity from ownerId->class + effectType instead of adding schema fields;
# (3) Stone Wall is not a cone/line — ABILITY_HIT_RANGE_PX.stonehide[0] is 0, so it resolves
#     self-centred at r100, and TAP abilities ignore their hitRange entirely.
# Two cross-cutting risks are recorded in every story rather than fixed here: App.tsx's
# latestTransientDelta is a single state value (React 18 batching collapses same-task deltas),
# and 7.1's VfxEngine is still unconsumed, so whichever story ships first also does the
# one-time DungeonScreen wiring (each story specifies it guarded "if not already present").
# 2026-07-21: ran epic-1-retrospective (done) — first retrospective run on the project,
# retroactively (Epics 1-6 all already complete by the time it ran). Synthesized findings
# from stories 1.1-1.10's dev notes/code-review history rather than fresh memory, since
# Epic 1 is pure infrastructure with no UI surface to recall. 2 action items created:
# (1) isHost flag has no server-side auth check (security debt open since 1.3, relevant
# again given Epic 8's registered-accounts scope), owner Simulation Engineer; (2)
# apps/mobile-controller has zero automated test coverage as of 1.10, owner Mobile
# Controller Engineer + QA support. Zod validation on deserialize() and the e2e
# reconnect.test.ts it.todo stub were surfaced but left at their existing deferred-work.md
# priority, not escalated. No epic-plan update needed — Epic 2 (the numerically next epic)
# is already done; actual next work was Epic 7 (backlog, no stories yet) at retro time —
# since renumbered to Epic 8 by sprint-change-proposal-2026-07-21.md, which inserted a new
# Epic 7 (Ability & Environmental VFX Prototyping) ahead of it. Full retro saved:
# epic-1-retro-2026-07-21.md.
# 2026-07-20: code review of 5-9-epic-5-post-58-deferred-hardening (done) — Blind Hunter +
# Edge Case Hunter + Acceptance Auditor all ran. Acceptance Auditor found zero AC violations
# (all 8 ACs + all 4 Non-goals confirmed against the diff). Blind Hunter and Edge Case Hunter
# independently found the same real defect: boss branch's `bondEligiblePlayerCount >= 2`
# fallback conflated "never snapshotted" with "snapshotted at 0/1", silently reintroducing
# D-5.8-A for solo/duo-started runs (resolveVoteIfComplete has no >=2 floor) — fixed via
# `!== -1`. Edge Case Hunter also found `assignBond` still pairs against the live roster each
# bond-moment, so activeBonds could exceed a frozen maxAchievableBonds for rosters growing
# mid-run — user chose to fix now (`===` to `>=` in the boss branch). Both patched, 2 new unit
# tests added, full suite re-verified green. 4 other findings dismissed as noise.
# 2026-07-20: code review of 1-10-epic-1-post-19-deferred-hardening (done) — Blind Hunter +
# Edge Case Hunter + Acceptance Auditor all ran; Acceptance Auditor found zero AC violations
# (all 5 ACs + all 4 non-goals confirmed against the diff). Edge Case Hunter surfaced 2 real,
# narrow latent risks introduced by the fix itself: D1 leavingIntentionallyRef is a single
# global (non-session-scoped) flag, so a rapid leave-room-A-then-join-room-B race can
# mis-attribute B's later disconnect; D2 safeReplaceState's blanket catch means
# handleGiveUp's stale-URL clear can silently no-op under the exact Safari throttle
# condition AC1 was built to survive. Both are net improvements over pre-story behavior,
# require compound-rare conditions to trigger, and a full fix would need a session-scoped
# handler / URL-independent state respectively — both explicitly out of this story's
# Non-goals (single boolean ref, no event bus). Deferred to deferred-work.md rather than
# patched. Blind Hunter's other 7 findings were dismissed on triage — most matched the
# story's own AC1/AC2/AC3 wording exactly (blanket catch, clearPersistedSession-before-
# disconnect ordering, asymmetric URL handling) or were pre-existing/out-of-scope
# (handleGiveUp never called session.disconnect() before this story either) — the reviewer
# had no project context by design (blind layer) and couldn't see the spec matched intent.
# Status: review → done.
# 2026-07-20: created 6 deferred-hardening stories in one batch (1.10, 2.9, 3.24, 4.14, 5.9,
# 6.10), one per epic with a genuine backlog since its last hardening pass — closing the
# "for all unresolved deferred items" sweep of deferred-work.md's 278 logged findings.
# Each story's candidate findings were re-verified against current source before being
# scoped in (per the established 3.22 precedent), not trusted from deferred-work.md's text
# alone; several were dropped or re-deferred where re-verification found them stale,
# already resolved, or still below their own stated revisit threshold. Reopens epic-1 and
# epic-5 (both previously "done") back to in-progress, matching this project's established
# reopen-for-leftover-deferred-findings pattern. 1.10 (epic-1, mobile session/back-nav
# races), 2.9 (epic-2, hub/controller cleanup + 2.8 test-gap findings), 3.24 (epic-3,
# primary task closes the user-flagged D-3.23-A/D-3.3-B skill-cell touch-teardown bug plus
# 3.22's e2e-hygiene/shield-guard leftovers), 4.14 (epic-4, loadLevel ordering-risk +
# resetToHub body-leak + isFrozen-on-transition + VotePopup stuck-button hardening), 5.9
# (epic-5, single finding: maxAchievableBonds roster-snapshot bug), 6.10 (epic-6,
# selectBondPair re-pair fix + re-deferrals for the physics-layer projectile-vs-boss gap
# and the cosmetic boss double-hit-per-tick). All 6 are ready-for-dev; none implemented yet.
# 2026-07-20: code review of 3-23-skill-cell-joystick-aiming-interaction (review →
# in-progress) — Blind Hunter + Edge Case Hunter + Acceptance Auditor all independently
# converged on the same 2 real bugs, both now fixed: (1) the onTouchMove cell-rect clamp
# box-clamped X/Y independently before computing direction, distorting the committed aim
# angle for any drag that exits the compact cell (the common case, not an edge case) — fixed
# by computing direction from the unclamped touch-to-origin vector (reverting to pre-3.23
# math), relying on the pre-existing SKILL_JOYSTICK_RING_RADIUS knob clamp + the cell's
# overflow:hidden for the visual boundary clip instead; (2) the knob snapped to center on
# deadzone re-entry while lastDirX/lastDirY (and thus AUTO/AIM_CAST's continued fire) kept
# the prior committed direction — fixed by leaving knobOffset untouched below the deadzone
# instead of resetting it. A 3rd finding (zero-size getBoundingClientRect mid-touch
# deadlocking direction) was resolved as a side effect of fix (1). Typecheck clean
# (apps/mobile-controller). 2 items deferred to deferred-work.md (D1: required Client-UX
# hook still not run — no display in this sandbox, own code trace corroborates a plausible
# text-occlusion risk from the 80px ring vs ~80-100px cell height; D2/D-3.23-A: ring/knob
# tears down mid-hold on cooldown start, pre-existing isInteractive dependency, out of this
# story's Non-goals). Status set to in-progress, not done, pending the human Client-UX pass.
# 2026-07-20: implemented 3-23-skill-cell-joystick-aiming-interaction (review) — ring+knob
# aiming visual added to SkillCell for AUTO/RELEASE/AIM_CAST held input types (80px ring/
# 28px knob, mirroring the movement joystick's ref+state pattern), a new dedicated 10px
# SKILL_CELL_DEADZONE_RADIUS replacing the undocumented inline DEADZONE=6, a single
# onTouchMove clamp-to-cell-rect step satisfying both the ring/knob positioning and the
# cell-boundary rule at once, a new skill-cell-pulse 1.2s keyframe distinguishing AIM_CAST
# from AUTO (both share accent-spirit's identical hex, so pulse — not color — is the
# differentiator per DESIGN.md), and the AIM_CAST badge color fix (accent-warm →
# accent-spirit) at both ABILITY_BADGE_BORDER and the live grid's inline ternary. No fire
# cadence/timing/protocol change. Typecheck clean (all 10 monorepo tsconfig projects). Full
# suite: 436 passed, 0 regressions; 3 pre-existing failures unrelated to this story's
# mobile-controller-only scope (ability-dispatch/hub-ability-use e2e: simulation-server
# port-binding timeout; full-run: the already-tracked D1 selectBondPair re-pair flakiness
# from 2026-07-03, same pattern noted in 6.9's history above). Manual Client-UX
# verification (per-type ring/knob color+animation+gesture check, minimal-attention
# readability) NOT performed — no display/touch device in this sandbox, matches dev-3's
# precedent; needs a human pass before this story can be marked done. New finding logged
# (not fixed, out of scope): D-3.23-A — AUTO/AIM_CAST's ring+knob and touch tracking tear
# down the instant cooldown starts (isInteractive includes !isOnCooldown and is a dependency
# of the touch-effect), not on lift as EXPERIENCE.md §4 specifies, orphaning every
# successful cast's still-held touch until the player lifts and re-touches. Same root cause
# as the already-tracked D-3.3-B, much higher frequency (every AUTO/AIM_CAST cast).
# 2026-07-20: started work on 3-23-skill-cell-joystick-aiming-interaction.
# 2026-07-20: epic-3 reopened to in-progress — created 3-23-skill-cell-joystick-aiming-
# interaction (ready-for-dev), a UX-spec-drift closure from the 2026-07-20 UX update
# session (decision-log.md D-017 through D-020). Not a new PRD FR: extends the skill-cell
# joystick ring+knob visual (already prototyped in mockups/controller-landscape-1.html but
# never carried into the spec text or the build) to all 3 held AbilityInputTypes (AUTO,
# RELEASE, AIM_CAST — the 4th type added by Story 3.11, given real channel semantics by
# 3.18, but never given its own aiming visual), adds a distinct pulse so AIM_CAST doesn't
# read as a plain AUTO repeat, gives skill-cell aiming its own dedicated 10px deadzone
# constant (was an undocumented inline 6px, separate from the movement joystick's 8px), and
# fixes ABILITY_BADGE_BORDER/inline badgeBorderColor's AIM_CAST color (a pre-3.18 leftover
# still grouping it with RELEASE's accent-warm instead of AUTO's accent-spirit family).
# Single-owner: Mobile Controller Engineer, 100% within apps/mobile-controller/**.
# 2026-07-17: created 6-9-epic-6-post-66-deferred-hardening (ready-for-dev) — scopes
# the two open combat-critical deferred findings since 6.6: D-dev4-B (user-flagged
# HIGH PRIORITY — shared resolveOutgoingDamage() helper so BOND_DAMAGE_MULT/
# DEBUG_GOD_MODE_DAMAGE_MULT apply uniformly across all 5 damage-delivery sites,
# not just hit-scan/Spirit Nova) and D-dev5-A (Storm Eye's zone-tick damage and
# bonus strike never reach the boss — adds a direct isInHitZone-against-
# boss.position check, mirroring Spirit Nova's existing bossInRing pattern, since
# the boss's physics fixture has filterMaskBits:0 and can never appear in
# zoneOverlapping contact sets). Explicitly out of scope: projectile-vs-boss
# collision (physics-contact-layer gap, deeper than a hardening story), the
# eslint.config.mjs repo-wide lint gap (D3, different ownership area), and the
# host-session.ts whitelist fragility (D-dev5-D, not yet at its stated revisit
# trigger). Simulation Engineer single-owner story.
# 2026-07-17: implemented 6-9-epic-6-post-66-deferred-hardening (review) — added
# resolveOutgoingDamage(rawDamage, isBonded, isGodMode) to packages/game-rules/src/balance.ts;
# routed all 5 GameRoom.ts damage-delivery sites (hit-scan, Spirit Nova, zone damage-tick, Storm
# Eye bonus strike, projectile hit) through it, replacing each site's local inline mult/damage
# ternary. Added boss branches to the zone damage-tick and Storm Eye bonus-strike loops (both use
# the same isInHitZone-against-boss.position pattern Spirit Nova's bossInRing already used, since
# the boss's filterMaskBits:0 fixture never appears in zoneOverlapping). Removed now-unused
# BOND_DAMAGE_MULT/DEBUG_GOD_MODE_DAMAGE_MULT imports from GameRoom.ts. New unit test
# (balance.test.ts, 5 cases) covers no-buff/bond-only/god-mode-only/stacked/round-only-when-mult!==1.
# Typecheck clean across the full monorepo. Full test suite: 436 passed, 0 regressions; 3 e2e
# files failed, all matching dev-4's documented pre-existing-environment-failure precedent
# (ability-dispatch/hub-ability-use: simulation-server port-binding timeout; full-run: hit the
# already-tracked D1 selectBondPair re-pair bug from 2026-07-03, confirmed via 4 repeated runs
# against this story's changes vs 3 against unmodified baseline — same intermittent pattern on
# both, unrelated to this story's changes). D-dev4-B and D-dev5-A marked RESOLVED in
# deferred-work.md; new findings D-6.9-A (projectile-vs-boss still unreachable, physics-layer,
# out of scope) and D-6.9-B (full-run.test.ts flakiness root-caused to pre-existing D1) logged.
# 2026-07-17: code review of 6-9-epic-6-post-66-deferred-hardening (done) - 3 parallel layers
# (Blind Hunter, Edge Case Hunter, Acceptance Auditor). 0 decision_needed, 3 patch, 2 defer,
# 6 dismissed as noise (incl. the intended AC2 multiplier-coverage expansion to zone-tick/
# projectile sites, correctly recognized as the story's stated purpose, not scope creep).
# Patches applied: Storm Eye boss-strike zone:strike broadcast now sends the boss's real id
# instead of the internal '__boss__' sentinel (was leaking into the ZoneStrikeDelta.targetId
# wire field); AC7's rounding unit test changed from an integer to a fractional input (was
# tautological, could not distinguish conditional-round from always-round); file-mode patch
# skipped as a WSL2 DrvFs mount artifact (all files report 777, core.fileMode=false, not a
# real diff). Deferred: D-6.9-C (boss can take 2 damage hits in 1 tick - pre-existing
# dual-hit-per-tick pattern extended to boss, cosmetic double-broadcast at worst) and D-6.9-D
# (isInHitZone boss-range check duplicated in 2 places - spec-prescribed, revisit at 3rd
# occurrence). Full monorepo typecheck clean after patches.
# 2026-07-17: implemented dev-4-debug-invincible-high-damage-mode (review) — debug:toggle-god-mode
# message added to the existing NODE_ENV-gated debug block (GameRoom.ts), self-targeting only via
# client.sessionId, room-local godModePlayerIds Set (no shared-types/net-protocol touch). Gates
# all 5 incoming-damage paths (Dark Pact drain, enemy melee, Fate Bond wipe, boss stomp, bond
# drain) to zero and multiplies outgoing hit-scan/mixed-faction + Spirit Nova damage by new
# DEBUG_GOD_MODE_DAMAGE_MULT=10 (balance.ts), stacking with BOND_DAMAGE_MULT. Typecheck clean,
# full unit/contract suite green (425 passed, 0 regressions). 4 e2e test files fail in this
# sandbox (simulation-server never binds a port here — confirmed pre-existing/environmental via
# stash/pop, identical failure on baseline). No new automated test (matches debug:kill-all/
# debug:kill-boss's zero-coverage precedent). Code review (3 parallel layers): 0 decision_needed,
# 1 patch, 1 defer, 13 dismissed as noise. Patch applied: onLeave's grace-period-expiry catch
# block now also cleans up godModePlayerIds (was only done in the CONSENTED branch, unlike every
# sibling field). 1 finding deferred (D-dev4-A: no guard against a stray debug message reaching
# a production server — pre-existing, shared with debug:kill-all/debug:kill-boss). Post-review
# addendum (user-requested): added a dev-build-only mobile button (ControllerScreen.tsx,
# mobile-session.ts) to actually trigger the toggle, since the story shipped with no client
# trigger at all. User live-tested and found Storm Eye's damage isn't multiplied — root cause:
# damage is computed independently per ability-delivery type across 5 GameRoom.ts sites, only 2
# of which apply any multiplier (BOND_DAMAGE_MULT has the identical pre-existing gap). Logged as
# D-dev4-B in deferred-work.md — user flagged this HIGH-PRIORITY/foundational (affects every
# future multiplicative system: buffs, debuffs, gear, crit), recommend picking up soon as its
# own properly-scoped Simulation Engineer story (shared damage-resolution function), not left
# indefinitely like other deferred items.
# 2026-07-17: dev-3-controller-fullscreen-toggle (done) — auto-fullscreen on first join and
# reconnect (T1/T2, later reverted after live-test — see below) and fullscreen toggle button
# with fullscreenchange sync (T3) in ControllerScreen.tsx. Typecheck clean throughout ("lint
# clean" claimed at the time was wrong — rtk was serving stale cached lint output; the real
# repo-wide lint state has 308 pre-existing errors from a missing eslint globals config,
# logged as D3 in deferred-work.md, unrelated to this story). Code review (3 parallel layers):
# 0 decision_needed, 1 patch (applied — `?.` guards added to toggleFullscreen's exit/request
# calls), 2 defer (bare-div toggle a11y gap mirrors pre-existing InteractButton convention;
# fullscreen persistence across screen transitions is an unspecified, plausibly-correct
# default), 19 dismissed as noise. No AC violations. No automated tests (Fullscreen API
# needs a real user gesture, unreliable in jsdom — same precedent as dev-1/dev-2). User
# live-tested: desktop Chrome auto-fullscreen rendered a blank page (phone was fine) — T1/T2
# reverted, AC1-3/AC7 struck, manual toggle (T3) unaffected. iPhone toggle didn't work either
# — confirmed as a real WebKit platform limitation (no Fullscreen API for DOM elements at
# all), not a bug; added T4/AC9, a tap-to-reveal "Add to Home Screen" hint for iOS Safari
# tabs (`navigator.standalone === false`) in place of the dead toggle. Manual
# Client-UX hook checks (reconnect-while-not-fullscreen, denied-in-iframe, joystick/grid
# non-overlap) still need a human pass on a real device — not verified in this session (no
# display in the dev environment).
# 2026-07-16: code review passed for dev-5-boss-transient-delta-whitelist-fix (done) — 0
# decision_needed, 1 patch, 4 defer, 9 dismissed as noise. Patch applied: Task 2's test
# checkbox reworded to state the actual 34/37-file result instead of an unqualified "full
# suite green" claim. 4 defer findings logged as D-dev5-B..E in deferred-work.md
# (single-slot transient-delta state can drop a delta under same-tick collisions, no
# runtime payload validation, whitelist-pattern structural fragility, and a reproduction
# of the already-tracked D7 story-template Allowed-paths gap — none block this story).
# 2026-07-16: completed dev-5-boss-transient-delta-whitelist-fix (review) — 4-line
# onTransientDelta whitelist addition in host-session.ts (boss:phaseChanged/damaged/
# stomped/defeated), closing D-6.8-A. Automated checks (typecheck/build/full test suite)
# all pass. Manual live-verification performed by the user directly (no browser/display
# in the dev environment) — confirmed phase glow, stomp ring, and full defeat/
# purification-pulse/reward-reveal/achievements sequence all work; damage flash confirmed
# for every direct-hit ability. Verification surfaced one new, unrelated, out-of-scope bug
# (logged as D-dev5-A, not fixed here): Stormcaller's Storm Eye zone deals zero damage to
# the boss because GameRoom.ts's zone-tick damage loop only ever checks gameState.enemies,
# never gameState.boss — Story 6.7 wired direct hit-scan abilities to the boss but never
# extended that to zone-tick resolution. Simulation Engineer / GameRoom.ts territory, out
# of this story's Blocked paths — needs its own follow-up story.
# 2026-07-15: created 3.22 (epic-3-post-321-deferred-hardening) — closes the Epic 3
# deferred-work.md backlog that accumulated since 3.10 (Stories 3.11-3.21c never had a
# hardening pass). Re-verified every candidate D-3.x entry against current source before
# scoping: 6 real, actionable findings bundled in (D-3.16-A, D-3.17-A/B/C, D-3.20-A,
# D-3.13-D); 2 dismissed on re-inspection as no longer matching the code (D-3.14-B's
# "overwrite" premise — current applyDisplacementTo* already does `+=`, not assignment,
# so concurrent pulls already sum) or as revisit-condition-still-unmet (D-3.13-A —
# 3.19/3.20 never actually gave projectiles per-ability visual identity, the condition
# this finding names to act on); D-3.18-A and the three Phase-5-scoped 3.21a/b findings
# left deferred, unchanged, per their own explicit conditions. The main task reuses the
# existing tests/e2e live-room harness (built for 4.6) to close a gap 4 separate code
# reviews (3.16, 3.17 x2, 3.20) each flagged independently: no test anywhere fires a real
# ability against a live GameRoom and asserts on the resulting deltas — full-run.test.ts
# bypasses all combat via debug:kill-all/debug:kill-boss, and every game-room-*.test.ts
# file mirrors GameRoom's logic in standalone functions rather than instantiating a room.
# 2026-07-14: implemented 3.19 (pending code review) — Souldrinker's full kit
# (Blood Spike projectile delivery + self-cost + 50% lifesteal, Crimson Lash
# inverse-HP damage scaling, Dark Pact single-target ally drain + self
# damageBuff, Void Pulse projectile impact + chained pull zone). First story
# to wire a ProjectileState-spawning ability (added ABILITY_DELIVERY
# declarative table) and the first to implement the zone-tick 'pull'
# effectType (left as a stub since 3.13). No new net-protocol wire type
# added for projectile spawn — reuses the existing 'add:spawned'-style full
# SNAPSHOT broadcast precedent instead. Corrected two wrong assumptions
# found by re-reading the current code rather than trusting the story text:
# (1) self-cost HP was never actually applied by any GameRoom.ts caller
# despite dispatchAbility computing it since 3.15 — added the missing
# generic wiring; (2) Dark Pact's ABILITY_HIT_RANGE_PX was 0, which would
# have made its ally-target search direction-independent — bumped to a
# nonzero forward-aim range matching Soul Mend's (3.18) convention. Resolved
# two previously-deferred findings at their first real call site:
# D-3.14-A (player-side pull liveness guard) and D-3.15-A's remaining
# caster-only case (Blood Spike lifesteal skips a downed/spirit caster).
# D-3.14-B (no vector-summing for concurrent pull sources) remains open,
# noted but not needed by any current caller. Updated 2 pre-existing tests
# (3.11/3.16) that asserted blanket all-zero balance tables, now stale for
# Souldrinker's slots. Full suite: typecheck 0 errors; vitest 459 passed, 12
# pre-existing skips, 0 failed at the test level (3 e2e test files errored
# on EADDRINUSE from concurrent other-agent worktree sessions competing for
# fixed ports on this machine — confirmed unrelated to this story).
# 2026-07-14: correct-course ran against TODO.md (sprint-change-proposal-2026-07-14.md,
# approved). Reopened epic-2, epic-4, epic-6 — none were new scope: 2.8 supersedes
# Story 2.4's training-dummy-only ability gate; 4.12/4.13 are net-new asks (HP restore,
# vote button state) with no prior AC; 6.7/6.8 are regressions against Story 6.1/6.3's
# already-approved AC (boss:damaged protocol/host code exists but was never wired —
# every hit-resolution loop in GameRoom.ts only checked gameState.enemies, never
# gameState.boss). Appended 3.21a/b/c to epic-3 (already in-progress, no reopen) — the
# body/spirit entity split, split by ownership (Protocol Architect/Simulation
# Engineer/Host Experience Engineer) per CLAUDE.md, sequenced a->b->c; 3.21b must
# regression-test Soul Mend (3.18) once body and spirit positions diverge. Added
# dev-2/dev-3/dev-4 to the no-epic bucket (rotation lock, fullscreen toggle, debug
# god-mode), same category as dev-1. Excluded the placeholder-VFX TODO.md item —
# sentence is incomplete/unscoped, needs the user to clarify before it can be triaged.
# 2026-07-14: completed 3.18 (done) — hold-to-channel Soul Mend, replaces
# 3.11's temporary AIM_CAST bridge. New PlayerState.channelingAbility field +
# cast:started/cast:cancelled/cast:completed deltas (contract-change hook
# triggered). Code review found 6 patch findings (missing cast:completed
# signal, channelingAbility leak through resetToHub, CLASS_SELECT mid-channel
# corrupting range/cooldown lookups, no caster-incapacitation check, client-
# clock startedAt skew, misleading comment) — all fixed. 1 finding deferred
# (D-3.18-A: no tie-break when two casters target the same downed ally —
# self-corrects, no invalid state, just an unfair join order).
# 2026-07-14: completed 3.17 — code review found 2 patch findings (Spirit Nova
# sweep missing the Bond proximity-damage buff Ancestor's Voice already has;
# Spirit Nova sweep missing a dungeon-phase guard, unlike every sibling
# per-tick combat block), both fixed. 1 finding escalated to a decision
# (Warding Cry's shield status effect applies but its damage-absorption
# consumption isn't wired — outside Allowed paths, absorption model
# undecided) — user chose to defer to a follow-up rather than expand scope
# (deferred-work.md D-3.17-A). 4 more findings deferred as pre-existing/
# batch-wide/minor (D-3.17-B..E: no GameRoom integration tests — same gap as
# D-3.16-A; a unit test that validates a reimplementation instead of the
# shipped sweep logic; activeSpiritNovas cleared without a completion event
# on level transition; resolveExpandingRadius's unreachable divide-by-zero).
# 8 findings dismissed (false-premise or by-design, incl. one Blind-Hunter
# claim disproven by checking the pre-existing Avalanche ability). Full suite
# re-run clean after patches: 473 passed, 0 regressions. Outcome: Approved.
# 2026-07-14: implemented 3.17 (pending code review) — Spiritcaller's full kit
# (Ancestor's Voice mixed-faction cone, Spirit Nova expanding-radius
# mixed-faction sweep, Warding Cry allies-in-zone shield status effect). Added
# GameRoom.ts#gatherPlayersInHitZone — the first query in the codebase to
# gather nearby PLAYERS for a hit zone (every prior ability only targeted
# enemies) — reused by all three abilities. Resolved deferred-work.md
# D-3.15-A/D-3.15-B at the call site (gather query excludes downed/spirit/
# frozen players and dead enemies before resolveMixedFactionTargets/
# healPlayer ever see them; neither function itself changed). One judgment
# call: ABILITY_DAMAGE.spiritcaller[0] bumped 0→15 (was an inert placeholder
# — AC1 requires enemies actually take damage). One deliberate scope
# boundary: Warding Cry's shield status effect applies but its damage-
# absorption consumption is NOT wired (packages/game-rules/src/systems/
# player-health.ts is outside this story's Allowed paths; logged as
# deferred-work.md D-3.17-A rather than silently expanding scope). 10 tests
# added (5 tests/unit/targeting.test.ts, 5 tests/unit/abilities.test.ts); full
# suite (excl. pre-existing e2e tests needing live Docker/Redis) 473 passed,
# 0 regressions.
# 2026-07-13: completed 3.16 — Stonehide's full kit (Iron Skin self-scope
# damageReduction, Tremor Stomp AoE damage+slow, Stone Wall damage+pull via
# ABILITY_STATUS_EFFECT/ABILITY_DISPLACEMENT_STRENGTH declarative tables in
# balance.ts; Avalanche unchanged). Verified during implementation that
# status:applied's existing shape covers both player/enemy targets, so no
# packages/net-protocol/** change was needed — narrower footprint than the
# story header's "Multi-context" caveat allowed for (Simulation Engineer
# only). Code review: 1 patch fixed (Stone Wall's enemy displacement wasn't
# broadcast to clients — applyDisplacementToEnemy now mirrors
# applyDisplacementToPlayer's body-reposition + broadcast), 1 minor test
# patch, 1 finding deferred (deferred-work.md D-3.16-A, GameRoom.ts
# integration test coverage — batch-wide gap, not unique to this story),
# rest dismissed. 6 tests total added to tests/unit/abilities.test.ts; full
# suite (excl. pre-existing e2e tests needing live Docker/Redis) 462
# passed, 0 regressions.
# 2026-07-13: completed 3.15 — self-cost HP mechanism, inverse-HP damage
# scaling, lifesteal helpers, mixed-faction target resolution. All zero-value
# until 3.19 (self-cost/HP-scaling/lifesteal) and 3.17 (mixed-faction) wire
# actual abilities. One scope exception: the additive AbilityDispatchContext
# fields broke a pre-existing dispatchAbility call in GameRoom.ts (outside
# this story's allowed paths) — user approved a minimal 2-line fix to keep
# it compiling; no new behavior wired there. Code review: 1 patch fixed
# (calculateHpScaledDamage divide-by-zero guard), 2 findings deferred
# (deferred-work.md D-3.15-A healPlayer downed-state guard, D-3.15-B
# resolveMixedFactionTargets dead/downed filtering) — both unreachable until
# 3.17/3.19 wire an actual caller, 7 other findings dismissed as by-design
# or environmental noise.
# 2026-07-13: started work on 3.15.
# 2026-07-13: completed 3.14 — code review found 3 patch findings (epsilon
# guard for near-zero displacement distance, an overclaiming comment about
# same-tick enemy body sync, an unconditional zero-displacement broadcast),
# all fixed; 2 findings deferred to deferred-work.md (D-3.14-A liveness/state
# guard, D-3.14-B concurrent-source accumulation) — both belong to 3.16/3.19
# once an actual ability wires a caller. body.setPosition bypassing collision
# was raised but dismissed as spec-mandated (AC4).
# 2026-07-13: completed 3.12 — code review found 3 patch findings (magnitude
# validation, status:applied wire payload missing magnitude, redundant
# Date.now() calls), all fixed; 2 minor findings deferred to deferred-work.md
# (D-3.12-A, D-3.12-B).
# 2026-07-13: started work on 3.12.
# 2026-07-11: created 3.12-3.20 in one batch, completing the "Ability
# Mechanics Rework" extension started by 3.11 (epics.md Stories 3.11-3.20).
# Dependency order: 3.11 -> {3.12,3.13,3.14,3.15 in any order, all shared
# engine capability, no visible gameplay change} -> 3.16 -> 3.17 -> 3.18 ->
# 3.19 -> 3.20 (kit-rework stories are sequenced; each reuses tables/helpers
# the previous ones introduced — see each story's Dev Notes for exact
# cross-story dependencies). Several stories carry corrections to epics.md's
# literal AC wording where it didn't match the current codebase (wrong file
# path in 3.11 carried forward as a pattern-check; 3.14's displacement must
# NOT use body.applyLinearImpulse — verified inert in this architecture,
# use direct position mutation instead — see 3.14's Context section).
# 2026-07-11: epic-3 reopened to in-progress (again) — new scope, not deferred
# hardening: the 2026-07-08 brainstorming session designed the "Ability Mechanics
# Rework" extension (epics.md, Stories 3.11-3.20) fixing 7 placeholder/no-op
# abilities and 1 mislabeled one. Created 3.11, the first of 5 shared-engine-
# capability stories the kit reworks (3.16-3.20) depend on.
# 2026-07-07: epic-1 reopened to in-progress — 2 deferred findings from the
# 1.8 code review (deferred-work.md) had no follow-up story; created 1.9 to close them.
# 2026-07-07: epic-2 reopened to in-progress — D-2.4-A from the 2.4 code review
# (deferred-work.md) was never picked up despite its own stated revisit condition
# ("Story 3.x when cooldowns persist into dungeon runs") having been met since; created
# 2.7 to close it.
# 2026-07-07: epic-3 reopened to in-progress — 2 deferred findings from the 3.9 code
# review (deferred-work.md, D-3.9-A, D-3.9-B) had no follow-up story; created 3.10 to
# close them.
# 2026-07-07: epic-5 reopened to in-progress — 3 deferred findings from the 5.7 code
# review (deferred-work.md, D-5.7-A/B/C) had no follow-up story; created 5.8 to close them
# (D-5.7-A verified as already fixed by 5.7's own review patch — 5.8 only adds a
# regression test for it; D-5.7-B narrowed per explicit Product/Game Design decision).
project: party-delve
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  # ─── E1: Foundation Platform ────────────────────────────────────────────────
  # Players can create a session on the host screen and join from their phones
  # via QR code or session code. They appear in an empty hub world.
  # Disconnect/reconnect within grace period works.
  # FRs: FR1, FR2, FR3 (guest join), FR27, FR28, FR31, FR32, FR33, FR37, FR38
  epic-1: done
  1-1-monorepo-architecture-clean-slate-and-package-scaffold: done
  1-2-simulation-server-session-lifecycle-and-30hz-tick-loop: done
  1-3-host-app-main-menu-and-lobby-screen: done
  1-4-mobile-app-guest-join-flow: done
  1-5-hub-world-bootstrap-and-player-presence: done
  1-6-disconnect-grace-period-and-reconnect-flow: done
  1-7-epic-1-deferred-hardening: done
  1-8-epic-1-post-17-deferred-hardening: done
  1-9-epic-1-post-18-deferred-hardening: done
  # Closes 1.9's own review leftovers (D1-D3): unguarded history.replaceState at both
  # call sites, async onLeave/handleDisconnect race, handleGiveUp stale URL fallback.
  1-10-epic-1-post-19-deferred-hardening: done
  epic-1-retrospective: done

  # ─── E2: Hub World & Class Selection ────────────────────────────────────────
  # Players explore the tribe village hub, interact with POIs (class selection,
  # training dummy), browse and select a class, and confirm their choice.
  # FRs: FR5, FR11 (4 alpha classes defined), FR12, FR13
  epic-2: done
  2-1-hub-world-poi-layout-and-interact-button: done
  2-2-class-selection-flow-card-browse-and-selection: done
  2-3-class-confirmation-and-hub-controller-transition: done
  2-4-training-dummy-poi: done
  2-5-server-persistent-joystick-vector: done
  2-6-epic-2-deferred-hardening: done
  2-7-epic-2-post-26-deferred-hardening: done
  2-8-hub-ability-use-outside-training-dummy-poi: done
  # Closes D-2.6-A/B (rAF stale ref, spurious stopJoystick send) and D-2.8-A/C/D
  # (isInteractive inDungeon bypass, ability-guard regression test, raceTimeout dedup).
  2-9-epic-2-post-28-deferred-hardening: done
  epic-2-retrospective: optional

  # ─── E3: Core Combat — 4 Alpha Classes ──────────────────────────────────────
  # Stonehide, Spiritcaller, Souldrinker, and Stormcaller fully playable in
  # dungeon combat. Players use abilities, go down, get revived, enter spirit
  # form. "Clear" objective type is operational.
  # FRs: FR9(Clear), FR11, FR12, FR13, FR17, FR18, FR19, FR20, FR21, FR22, FR23
  epic-3: done
  3-1-xoshiro128-prng-and-planckjs-physics-world: done
  3-2-enemy-ai-base-fsm-and-layered-difficulty-behaviors: done
  3-3-4-alpha-class-implementations-abilities-and-input-types: done
  3-4-combat-resolution-hitboxes-damage-and-spirit-essence-collection: done
  3-5-player-health-revive-timer-and-downed-state: done
  3-6-spirit-form-downed-player-contribution-and-run-failure: done
  3-7-clear-objective-and-level-completion: done
  3-8-login-flow-update: done
  3-9-epic-3-deferred-hardening: done
  3-10-epic-3-post-39-deferred-hardening: done
  # Epic 3 Extension: Ability Mechanics Rework (scoped from the 2026-07-08
  # brainstorming session, not new PRD FRs) — 3.11-3.15 build shared engine
  # capabilities, 3.16-3.20 apply them per class. Sequenced so no story
  # depends on a later one.
  3-11-ability-input-taxonomy-expansion-and-type-corrections: done
  3-12-status-effect-engine-buffs-debuffs-with-duration: done
  3-13-projectile-physics-and-zone-field-entities: done
  3-14-displacement-pull-physics-primitive: done
  3-15-self-cost-resource-and-mixed-faction-target-resolution: done
  3-16-stonehide-kit-rework: done
  3-17-spiritcaller-kit-rework-ancestors-voice-spirit-nova-warding-cry: done
  3-18-soul-mend-ranged-spirit-targeting-revive: done
  3-19-souldrinker-kit-rework-blood-spike-crimson-lash-dark-pact-void-pulse: done
  3-20-stormcaller-storm-eye-rework: done
  # Epic 3 Correction: Downed Player Body/Spirit Entity Split (correct-course,
  # 2026-07-14, sprint-change-proposal-2026-07-14.md) — sequenced a->b->c
  3-21a-body-spirit-position-schema-and-protocol-contract: done
  3-21b-body-spirit-movement-and-revive-targeting-logic: done
  3-21c-host-rendering-distinct-body-and-spirit-entities: done
  # Post-3.21 hardening: closes deferred-work.md backlog accumulated by 3.11-3.21c
  # (never had its own hardening pass, unlike 3.1-3.9 which got 3.9/3.10)
  3-22-epic-3-post-321-deferred-hardening: done
  # UX-spec-drift closure (2026-07-20 UX update session, decision-log.md D-017-D-020) —
  # not a new PRD FR, see history note above
  3-23-skill-cell-joystick-aiming-interaction: done
  # Primary task closes user-flagged D-3.23-A (also fixes the older, related D-3.3-B):
  # SkillCell's touch-tracking effect tears down mid-hold on cooldown start instead of on
  # lift. Also closes 3.22's own review leftovers (e2e cleanup/raceTimeout hygiene, shield
  # magnitude guard) and 3.23's un-run Client-UX text-occlusion finding.
  3-24-epic-3-post-323-deferred-hardening: done
  epic-3-retrospective: optional

  # ─── Dev infra fixes (no epic) ──────────────────────────────────────────────
  dev-1-mobile-controller-network-binding: done
  dev-2-controller-rotation-lock-enforcement: done
  dev-3-controller-fullscreen-toggle: done
  dev-4-debug-invincible-high-damage-mode: done
  # Closes D-6.8-A (deferred-work.md): host-session.ts's onTransientDelta
  # whitelist is missing boss:phaseChanged/boss:damaged/boss:stomped/
  # boss:defeated, making the already-built 6.3/6.4/6.5 boss visual-reaction
  # code (damage flash, phase glow, stomp ring, purification pulse + reward
  # reveal + achievements list) unreachable dead code today.
  dev-5-boss-transient-delta-whitelist-fix: done

  # ─── E4: Procedural Dungeon & Full Run Structure ────────────────────────────
  # Vote to start a run, 3 procedural levels, deterministic seed system,
  # Survive the Waves objective, post-run summary.
  # FRs: FR6, FR7, FR8, FR9 (Survive Waves), FR10, FR19, FR29, FR30, FR34
  epic-4: done
  4-1-deterministic-seed-system-and-floor-layout-generator: done
  4-2-dungeon-entrance-vote-and-run-initialisation: done
  4-3-3-level-run-structure-and-level-transitions: done
  4-4-survive-the-waves-objective: done
  4-5-post-run-summary-screen: done
  4-6-end-to-end-run-e2e-test-and-latency-baseline: done
  4-7-session-url-sync-on-join: done
  4-8-forced-class-selection-on-join: done
  4-9-epic-4-deferred-hardening: done
  4-10-epic-4-post-49-deferred-hardening: done
  4-11-epic-4-post-410-deferred-hardening: done
  4-12-full-hp-restore-on-level-transition: done
  4-13-vote-accept-button-submitted-state: done
  # Closes D-4.11-A/B (loadLevel level-2/default branch commit-ordering risk, resetToHub
  # boss-body leak), D1-4.12 (isFrozen never cleared on level transition), D1-4.13
  # (VotePopup stuck-on-Waiting with no recovery timeout).
  4-14-epic-4-post-413-deferred-hardening: done
  epic-4-retrospective: optional

  # ─── E5: Spirit Bond System ─────────────────────────────────────────────────
  # Bond assigned at level end, buff+price pairs, in-canvas particle tethers,
  # 3 bonds accumulate across 3 levels.
  # FRs: FR14, FR15, FR16
  epic-5: done
  5-1-spirit-bond-shared-types-and-protocol-contracts: done
  5-2-bond-assignment-logic-and-deterministic-pair-selection: done
  5-3-per-tick-bond-effects-proximity-and-fate-bond-types: done
  5-4-bond-assignment-integration-at-level-completion: done
  5-5-host-bond-visualization-assignment-overlay-and-particle-tethers: done
  5-6-mobile-bond-card-and-continue-ux: done
  5-7-epic-5-deferred-hardening: done
  5-8-epic-5-post-57-deferred-hardening: done
  # Closes D-5.8-A: maxAchievableBonds read the live player roster at boss-start instead
  # of a snapshot taken when bonds were actually assigned.
  5-9-epic-5-post-58-deferred-hardening: done
  epic-5-retrospective: optional

  # ─── E6: Grassland Boss Encounter ───────────────────────────────────────────
  # Behavior-tiered boss (Easy/Normal/Hard), synthesizes E3 mechanics, purification
  # pulse, reward reveal, biome achievements.
  # FRs: FR21 (boss tiers), FR24 (reward reveal), FR35 (Grassland boss + achievements), FR36
  epic-6: done
  6-1-grassland-boss-shared-types-and-protocol-contracts: done
  6-2-grassland-boss-fsm-phase-system-and-difficulty-tiered-behaviors: done
  6-3-boss-arena-handcrafted-level-physics-geometry-and-host-rendering: done
  6-4-boss-defeat-sequence-purification-pulse-and-reward-reveal: done
  6-5-grassland-biome-achievements: done
  6-6-epic-6-deferred-hardening: done
  6-7-boss-combat-resolution-wiring: done
  6-8-floating-damage-numbers-regular-enemies: done
  6-9-epic-6-post-66-deferred-hardening: done
  # Closes D-6.9-B: selectBondPair can re-pair an already-bonded pair, causing full-run.
  # test.ts's 3-player bond-count assertion to flake. D-6.9-A/C re-deferred (physics-layer
  # projectile-vs-boss gap; cosmetic double-hit-per-tick); D-6.9-D dropped (no 3rd instance
  # of the duplicated isInHitZone check has appeared yet).
  6-10-epic-6-post-69-deferred-hardening: done
  epic-6-retrospective: optional

  # ─── E7: Ability & Environmental VFX Prototyping ────────────────────────────
  # Inserted by sprint-change-proposal-2026-07-21.md — shape/particle-based visual
  # identity for all 16 shipped abilities + Grassland boss attacks + status effects,
  # replacing today's undifferentiated flat-color circles, ahead of the final
  # PixelLab pixel-art pass. No new FRs. Pushed Progression/Full Class Roster/
  # Ancient Forest Biome/Polish down one slot each (old E7-E10 -> E8-E11).
  epic-7: in-progress
  7-1-vfx-engine-foundations: done
  7-2-stonehide-ability-vfx: done
  7-3-spiritcaller-ability-vfx: done
  7-4-souldrinker-ability-vfx: done
  7-5-stormcaller-ability-vfx: done
  7-6-status-effect-vfx: done
  # Epic 7.7 was split at story-creation time (2026-07-22, user-approved): the epic assumed
  # boss:charged only needed a host whitelist entry, but there is no BossChargedDelta in
  # net-protocol's DeltaEventMsg union at all and GameRoom.ts:1926 explicitly swallows the
  # event. Making it reach the host spans 3 ownership areas, so 7.7a confines the
  # protocol+sim work (Contract-change + Simulation-safety hooks) to one small story and
  # 7.7b stays host-only. 7.7b depends on 7.7a for its charge AC only; its three boss
  # reskin ACs are independent and ship regardless.
  7-7a-boss-charged-delta-contract-and-broadcast: done
  7-7b-grassland-boss-attack-vfx-and-charge-visual: done
  7-8-environmental-and-bond-vfx-polish: ready-for-dev
  # 7.9 (2026-07-23, user "Option B" decision): move ability spatial geometry from
  # game-rules/balance.ts into shared-types so the VFX tracks balance dynamically instead
  # of hand-transcribing it (resolves D-7.2-A). Second sanctioned Epic 7 contract-change
  # exception (after 7.7a). Multi-owner (Protocol Architect + Simulation Engineer + Host);
  # kept as one story because a constant relocation is atomic. Should land BEFORE 7.4/7.5/7.8
  # so they consume the contract rather than transcribe (Task 5 re-points their specs).
  7-9-shared-ability-geometry-contract: done
  # 7.10 (2026-07-23): defect found in the 7.4 playtest — projectiles simulate server-side but
  # have no projectile:moved delta, so the host dot freezes/teleports (only the 5 s snapshot syncs
  # it). Protocol + Simulation joint story (mirrors 7.7a); unblocks projectile visuals in 7.4/7.8.
  7-10-projectile-position-streaming: done
  epic-7-retrospective: optional

  # ─── E8: Progression & Persistent Meta ──────────────────────────────────────
  # Registered accounts, Spirit Essence persistence, mastery counters, hub shop.
  # Stories: not yet defined
  epic-8: backlog
  epic-8-retrospective: optional

  # ─── E9: Full Class Roster ───────────────────────────────────────────────────
  # Remaining 6 classes (Sunwarden, Wildshaper, Songweaver, Trailhunter,
  # Shadowstalker, Windwalker) bringing total to 10.
  # Stories: not yet defined
  epic-9: backlog
  epic-9-retrospective: optional

  # ─── E10: Ancient Forest Biome ───────────────────────────────────────────────
  # Second biome fully playable: enemy pool, boss, achievements, visual identity.
  # Stories: not yet defined
  epic-10: backlog
  epic-10-retrospective: optional

  # ─── E11: Polish, Performance & Telemetry ───────────────────────────────────
  # All NFRs verified by measurement; telemetry instruments key flows;
  # difficulty curve playtested and tuned.
  # Stories: not yet defined
  epic-11: backlog
  epic-11-retrospective: optional
