# mobile-controller

The mobile controller is the phone-based input surface for party-delve. Players open this app on their phones to join a local session and send input events (movement, skills) to the simulation server. The UI is intentionally minimal — players watch the host screen, not their phones — so the controller shows only what is necessary: a connection status indicator, a room join flow, and (in later phases) a joystick and skill buttons.
