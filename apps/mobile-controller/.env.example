# Override the sim server URL (e.g. for WSS in production or a non-standard dev topology).
# Do NOT set this for local LAN dev — the app derives the host from window.location.hostname.
# VITE_SIM_URL=ws://192.168.x.x:2567
